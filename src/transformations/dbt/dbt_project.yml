name: 'nordic_retail_dbt'
version: '1.0.0'
config-version: 2
profile: 'default'

model-paths: ["models"]

models:
  nordic_retail_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table